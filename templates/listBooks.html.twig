{% extends 'base.html.twig' %}

{% block content %}
    <div class="block-home-books">
        <h2 class="h2-home-titles">Books</h2>
        <div class="grid-books">
            {% for book in books %}
                <a href="/book/{{ book.id }}" class=links>{{ book.title }} ({{ book.editor }})</a>
            {% endfor %}
        </div>
    </div>
{% endblock %}
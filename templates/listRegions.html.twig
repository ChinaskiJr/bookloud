{% extends 'base.html.twig' %}

{% block content %}
    <div class="block-home-booklouds">
        <h2 class="h2-home-titles">Regions</h2>
        <div class="grid-booklouds">
            {% for region in regions %}
                <p><a href="/regions/{{ region.id }}" class="links">{{ region.geographicalArea }}</a></p>
            {% endfor %}
        </div>
    </div>
{% endblock %}

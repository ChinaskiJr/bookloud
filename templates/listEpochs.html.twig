{% extends 'base.html.twig' %}

{% block content %}
    <div class="block-home-booklouds">
        <h2 class="h2-home-titles">Epochs</h2>
        <h2 class="h2-home-newBook"><a href="/add-epoch" class="links">->&nbsp;New&nbsp;Epoch&nbsp;<-</a></h2>
        <div class="grid-booklouds">
            {% for epoch in epochs %}
                <p class="text-center"><a href="/epoch/{{ epoch.id }}" class="links">{{ epoch.epoch }}</a></p>
            {% endfor %}
        </div>
    </div>
{% endblock %}
